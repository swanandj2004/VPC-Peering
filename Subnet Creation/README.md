Create two subnets in VPC A & VPC B 
Create two internet gateways, attach the respective VPCs to it
Create two route tables, associate the two created subnets to it -> This gives internet access to the subnets
